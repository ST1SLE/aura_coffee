## 1. Failing regression test for admin items list category filter

- [ ] 1.1 [core-api] RED: Add `test_admin_items_list_filters_by_category` to `services/core-api/tests/test_menu_admin.py`. The test MUST use the existing `client`, `admin_headers`, and `barista_headers` fixtures (no `db_client`, no `migrated_db_session`, no `_IS_SQLITE` skip — seed data through the admin API itself so the test is SQLite-safe). Flow: (a) `POST /api/v1/admin/menu/categories` twice to create categories A and B, capturing their ids; (b) `POST /api/v1/admin/menu/items` four times to create two items in A and two items in B, capturing their ids; (c) assert `GET /api/v1/admin/menu/items` → 200 and returns all four seeded ids; (d) assert `GET /api/v1/admin/menu/items?category_id=<A.id>` → 200, returns exactly the two A-items (set equality on ids), excludes both B-items; (e) assert `GET /api/v1/admin/menu/items?category_id=999999` → 404 with `detail == "category not found"`; (f) assert `GET /api/v1/admin/menu/items?category_id=0` → 422; (g) assert `GET /api/v1/admin/menu/items?category_id=abc` → 422; (h) assert `GET /api/v1/admin/menu/items?category_id=<A.id>` with `barista_headers` → 200 with the A-filtered set. Expected red state against current `main`: sub-assertions (d), (e), (f), (g) fail because the route currently ignores `category_id` and returns all four items with HTTP 200.
