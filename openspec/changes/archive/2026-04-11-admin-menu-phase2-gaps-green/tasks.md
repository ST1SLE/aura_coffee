## 1. Backend schema — request body model

- [x] 1.1 [core-api] GREEN: In `services/core-api/src/core_api/schemas/menu.py`, add a new Pydantic model `MenuItemModifierSet(BaseModel)` with a single required field `modifier_ids: list[int]` and `model_config = ConfigDict(extra="forbid")`. Place the class near the other availability/action models (adjacent to `AvailabilityPatch`). → unblocks `admin-menu-phase2-gaps-red` task 1.1 scenario (c) (import path will now resolve in the router).

## 2. Backend service — set-replacement logic

- [x] 2.1 [core-api] GREEN: In `services/core-api/src/core_api/services/menu_admin.py`, add method `set_item_modifiers(self, item_id: int, modifier_ids: list[int]) -> MenuItem` to `MenuAdminService`. Behavior: (a) load the `MenuItem` via `self.get_item(item_id)` so it raises 404 via the existing path if missing AND comes back with `modifiers` eager-loaded; (b) deduplicate `modifier_ids` via `unique_ids = list(dict.fromkeys(modifier_ids))`; (c) if `unique_ids` is non-empty, run `mods = self.db.query(Modifier).filter(Modifier.id.in_(unique_ids)).all()` and if `len(mods) != len(unique_ids)` compute `missing = sorted(set(unique_ids) - {m.id for m in mods})` and raise `HTTPException(status_code=422, detail=f"unknown modifier ids: {missing}")`; (d) if `unique_ids` is empty, set `mods = []`; (e) assign `item.modifiers = mods`; (f) wrap `self.db.commit()` in try/except `IntegrityError` with rollback and `_handle_integrity(exc, "integrity error")`; (g) return `self.get_item(item_id)` so the caller receives a freshly reloaded row with both `size_options` and `modifiers` eager-loaded. → passes `admin-menu-phase2-gaps-red` task 1.1 scenarios (c), (d), (e), (f), (h).

## 3. Backend router — new endpoint

- [x] 3.1 [core-api] GREEN: In `services/core-api/src/core_api/routers/menu_admin.py`, add `MenuItemModifierSet` to the import block from `core_api.schemas.menu`, then add a new route: `@router.put("/items/{item_id}/modifiers", response_model=MenuItemResponse, summary="Заменить набор модификаторов у позиции меню")` decorating a function `def set_item_modifiers(item_id: int, body: MenuItemModifierSet, svc: _Svc) -> MenuItemResponse` that returns `MenuItemResponse.model_validate(svc.set_item_modifiers(item_id, body.modifier_ids))`. Place the route immediately after `set_item_availability` (line ~131) so all `items/{id}/*` action routes sit together. → passes `admin-menu-phase2-gaps-red` task 1.1 scenario (c) (the path now resolves).

## 4. Backend RBAC — route matrix entry

- [x] 4.1 [core-api] GREEN: In `services/core-api/src/core_api/rbac_matrix.py`, add an entry for `("PUT", "/api/v1/admin/menu/items/{item_id}/modifiers")` mapping to `{"admin"}` only. Place the entry in the same block as the other `/api/v1/admin/menu/items/...` entries. Do NOT add the path to `PUBLIC_ROUTES`. → passes `admin-menu-phase2-gaps-red` task 1.1 scenarios (i) and (j).

## 5. Backend verification

- [x] 5.1 [core-api] VERIFY: Run `pytest services/core-api/tests/test_menu_admin.py::test_admin_set_item_modifiers -xvs` and confirm every sub-assertion (a)-(j) passes on the SQLite default fast path (no `TEST_DATABASE_URL` set). Then run the full `pytest services/core-api/tests/test_menu_admin.py` and confirm no previously passing test regressed.

## 6. Frontend API client — new helper

- [x] 6.1 [web-admin] IMPL: In `web/admin/src/api/menu.ts`, add a new helper `export const setItemModifiers = (id: number, modifier_ids: number[]): Promise<MenuItemResponse> => put(\`/api/v1/admin/menu/items/${id}/modifiers\`, { modifier_ids });`. Place it in the "Menu items" section immediately after `setItemAvailability`. No new type export is needed — the request body is inlined and the response reuses `MenuItemResponse`.

## 7. Frontend — category sort_order edit input

- [x] 7.1 [web-admin] IMPL: In `web/admin/src/pages/Menu/CategoryList.tsx`, add `editSortOrder: string` to the edit state block (next to `editType`, line ~35), initialize it in `startEdit(cat)` (line 82-87) as `setEditSortOrder(String(cat.sort_order))`, render a narrow numeric `<Input type="number" min="0">` inside the edit row (the `<div className="flex gap-1">` at lines 174-197) labelled via placeholder `"#"` with `className="h-7 w-14"`, and in `handleSaveEdit` (line 89-112) parse via `const parsed = parseInt(editSortOrder, 10); const sort_order = Number.isNaN(parsed) ? cat.sort_order : parsed;` and include `sort_order` in the `updateCategory(cat.id, { ... })` body. After a successful response, sort the updated `next` array by `(a, b) => a.sort_order - b.sort_order || a.id - b.id` before calling `setCategories(next)` and `onCategoriesLoaded(next)`. Do NOT touch the create form — it continues to hardcode `sort_order: categories.length`.

## 8. Frontend — ModifiersPicker component

- [x] 8.1 [web-admin] IMPL: Create `web/admin/src/pages/Menu/ModifiersPicker.tsx` exporting a `ModifiersPicker` component. Props shape: `{ menuItemId: number; allModifiers: ModifierResponse[]; selectedIds: number[]; onChange: (nextIds: number[]) => void; disabled?: boolean; onError: (msg: string) => void; }`. Implementation: local `savingId: number | null` state; render an `<h3>` i18n title plus a vertical list of one `<label>` per entry in `allModifiers`, each containing a `<Checkbox>` (from `@/components/ui/checkbox`) and the bilingual name via `pickLang(m.name_ru, m.name_en, i18n.language)`; checkbox `checked = selectedIds.includes(m.id)`, `disabled = disabled || savingId !== null`. On toggle, compute `nextIds = checked ? selectedIds.filter(x => x !== m.id) : [...selectedIds, m.id]`, set `savingId = m.id`, call `setItemModifiers(menuItemId, nextIds)`, on success call `onChange(resp.modifiers.map(x => x.id))`, on `ApiError` with status 401 call `onError(t('common.sessionExpired'))` else `onError(t('pages.menu.modifiersPicker.errorGeneric'))`, finally clear `savingId`. When `disabled` is true, render a muted hint `t('pages.menu.modifiersPicker.saveFirstHint')` above the list and early-return `null` for the network call in the toggle handler. Do NOT import or use `listModifiers` — the component is pure-presentational over the `allModifiers` prop.

## 9. Frontend — wire picker into the item form dialog

- [x] 9.1 [web-admin] IMPL: In `web/admin/src/pages/Menu/MenuItemFormDialog.tsx`, add `modifiers: ModifierResponse[]` to the `Props` interface (line 17-24), add a new local state `const [selectedModifierIds, setSelectedModifierIds] = useState<number[]>(item?.modifiers.map(m => m.id) ?? [])`, reset it inside the `useEffect` at lines 62-69 alongside the existing state resets via `setSelectedModifierIds(item?.modifiers.map(m => m.id) ?? [])`. Import `ModifiersPicker` from `./ModifiersPicker` and `ModifierResponse` from `@/api/menu`. Render the picker in a new `<div className="border-t pt-4">` block placed immediately after the existing `SizeOptionsEditor` block (lines 264-273): `<ModifiersPicker menuItemId={editItem?.id ?? 0} allModifiers={modifiers} selectedIds={selectedModifierIds} onChange={(next) => { setSelectedModifierIds(next); if (editItem) onSaved({ ...editItem, modifiers: modifiers.filter(m => next.includes(m.id)) }); }} disabled={!isEdit} onError={onError} />`. Do not touch `handleSubmit` — modifier changes are saved independently by the picker.

## 10. Frontend — lift modifier loading into MenuPage

- [x] 10.1 [web-admin] IMPL: In `web/admin/src/pages/Menu/index.tsx` (the `MenuPage` component), add state `const [modifiers, setModifiers] = useState<ModifierResponse[]>([])`, fetch the list in a `useEffect` on mount via `listModifiers().then(setModifiers).catch((err) => { if (err instanceof ApiError && err.status === 401) onError(t('common.sessionExpired')); else onError(t('common.error')); })`, pass `modifiers={modifiers}` as a new prop to `<MenuItemFormDialog ... />` wherever it is rendered, and pass `modifiers={modifiers} onModifiersChange={setModifiers}` to `<ModifiersPanel ... />`. If `ModifiersPanel` currently owns its own modifier list, update it in a separate task (11.1) to accept these props and call `onModifiersChange` after its own CRUD mutations so the lifted state stays in sync.

## 11. Frontend — update ModifiersPanel to accept lifted state

- [x] 11.1 [web-admin] IMPL: In `web/admin/src/pages/Menu/ModifiersPanel.tsx`, change the `Props` interface to accept `modifiers: ModifierResponse[]` and `onModifiersChange: (next: ModifierResponse[]) => void`. Remove the local `modifiers` state and the mount `listModifiers()` call (lines 34, 44-53). Replace every `setModifiers(...)` call in `handleAdd`, `handleSaveEdit`, `handleDelete`, and `handleToggle` (lines 65, 99, 116, 127) with a call that computes the next array and invokes `onModifiersChange(next)`. Keep `loading` state if wanted, but since fetching is lifted it SHALL be removed. Keep all i18n keys and RBAC gating unchanged.

## 12. Frontend — i18n keys

- [x] 12.1 [web-admin] IMPL: Add new i18n keys to both `web/admin/src/locales/ru.json` and `web/admin/src/locales/en.json` (or wherever the admin locale files live — check `i18n` setup): `pages.menu.modifiersPicker.title` ("Модификаторы" / "Modifiers"), `pages.menu.modifiersPicker.saveFirstHint` ("Сохраните позицию, чтобы выбрать модификаторы" / "Save the item first to pick modifiers"), `pages.menu.modifiersPicker.errorGeneric` ("Не удалось обновить модификаторы" / "Failed to update modifiers"), and `pages.menu.categories.sortOrder` ("Порядок" / "Order"). Verify each key renders without a missing-translation warning in both locales.

## 13. Frontend verification

- [x] 13.1 [web-admin] VERIFY: Run `pnpm -C web/admin typecheck` (or the repo's equivalent `tsc --noEmit` entrypoint) and confirm zero TypeScript errors. Run `pnpm -C web/admin lint` and confirm zero new lint warnings in the files touched.

- [ ] 13.2 [web-admin] VERIFY: Start the admin SPA (`pnpm -C web/admin dev` or through `docker compose up`), log in as admin, navigate to Menu. Test scenarios: (a) click edit on an existing category, change `sort_order` from its current value to `0`, click save, confirm the category moves to the top of the list without a page reload; (b) open an existing menu item in the form dialog, confirm the modifiers picker renders below the sizes editor with the currently attached modifiers pre-checked; (c) toggle a modifier checkbox, confirm a network request fires to `PUT /api/v1/admin/menu/items/{id}/modifiers`, confirm the checkbox remains in its new state on 200 response, and confirm the item in the table behind the dialog reflects the new modifier set; (d) create a brand new menu item, confirm the modifiers picker is disabled until the first save, then becomes interactive after save; (e) open the modifiers panel, create a new modifier, open an item dialog, and confirm the newly created modifier appears in the picker list; (f) log in as barista, confirm the category list renders without the sort_order edit control (the edit action itself is already role-gated). If any regression in adjacent admin flows is observed (category CRUD, item CRUD, existing modifier panel behavior, size editor, stop-list toggle, login/logout), stop and investigate before reporting done.

- [ ] 13.3 [web-admin] VERIFY: Switch UI language between RU and EN while the item form dialog is open on an item with attached modifiers. Confirm the picker re-renders modifier names in the new language, the "save first" hint (if visible in create mode) is translated, and the category `sort_order` label updates.
