## 1. Checkout service skeleton

- [x] 1.1 [core-api] GREEN: Create `services/core-api/src/core_api/services/checkout.py` with module docstring, imports (`uuid`, `datetime.UTC`, SQLAlchemy `Session`, Redis types, `core_api.schemas.order.*`, `shared.models.*`, `shared.enums.*`, `core_api.services.cart.CartService`), and an `EmptyCartError` class → satisfies test `test_checkout_service.test_module_is_importable` and the 400-mapping tests in `test_route_orders.py`.
- [x] 1.2 [core-api] GREEN: Add twelve module-level stub functions in `checkout.py` with signatures from design Decision 5 (`validate_stop_list`, `validate_time_slot`, `validate_delivery_address`, `validate_min_delivery_amount`, `validate_promocode`, `compute_subtotal`, `apply_promocode`, `apply_loyalty_points`, `compute_delivery_fee`, `compute_order_total`, `compute_estimated_accrual`, `enqueue_payment_task`), each body raising `NotImplementedError("<name> not wired — owned by <capability>")` → satisfies the `patch("core_api.services.checkout.<name>")` contract used across ~30 tests in `test_checkout_service.py`.

## 2. `create_order` — pre-DB phase

- [x] 2.1 [core-api] GREEN: Add `create_order(user_id, request, redis_client, db_session)` function to `checkout.py` that reads the cart via `CartService(redis_client).get(user_id)` and raises `EmptyCartError("Корзина пуста")` when the cart is missing or `items` is empty → satisfies "Empty cart is rejected" scenario (requirement: Checkout service converts cart to order atomically).
- [x] 2.2 [core-api] GREEN: Extend `create_order` to call `validate_stop_list(cart_items, db_session)` then (when `request.type == DELIVERY`) `validate_delivery_address(request.delivery_address, db_session)` and `validate_min_delivery_amount(subtotal_estimate, request.delivery_address)`, then `validate_time_slot(request.scheduled_for, shop_settings)`, then (when `request.promocode_code`) `validate_promocode(...)` — all before any DB write → satisfies "Stop-list validator failure aborts before any DB write", "Delivery type without valid address is rejected", "Invalid promocode aborts the transaction".
- [x] 2.3 [core-api] GREEN: Extend `create_order` to execute the §7.2 pricing chain in order (`compute_subtotal` → `apply_promocode` → `apply_loyalty_points` → `compute_delivery_fee` → `compute_order_total` → `compute_estimated_accrual`) and store intermediate values in locals → satisfies "pricing chain order" test in `test_checkout_service.py`.

## 3. `create_order` — atomic DB phase

- [x] 3.1 [core-api] GREEN: Wrap the DB phase of `create_order` in `with db_session.begin():`, generate `idempotency_key = str(uuid.uuid4())` inside the block, and INSERT the `Order` row with correct `status` (CREATED if total>0 else PAID), `user_id`, `type`, `subtotal`, `discount_amount`, `points_used`, `delivery_fee`, `total`, `scheduled_for`, `delivery_address` (JSONB) → satisfies "Normal pickup order produces CREATED status" and "Loyalty-only payment (total = 0) commits Order as PAID".
- [x] 3.2 [core-api] GREEN: Inside the same transaction, for each cart line, SELECT the current `MenuItem` and `SizeOption` (fresh DB read per INV-014) and INSERT an `OrderItem` with `order_id`, `menu_item_id` (plain BigInteger, no FK), `size_option_id` (nullable), `quantity`, `unit_price`, `name_ru`, `name_en`, `size_label`, `size_price`, `modifiers_snapshot` (JSONB list of `{id, name_ru, name_en, price}`) → satisfies "Snapshot captures current DB prices, not Redis" and "Modifier snapshot persists even after modifier archival".
- [x] 3.3 [core-api] GREEN: Inside the same transaction, INSERT the `Payment` row with `order_id`, `amount = total`, `status = PAID` when total=0 else `PENDING`, `idempotency_key`, `method = "yukassa"`, `created_at = now()` → satisfies "one `payments` row with `status = PENDING`, `amount = total`, and a non-null `idempotency_key`" and the total=0 scenario's `payments.amount = 0` assertion.
- [x] 3.4 [core-api] GREEN: Inside the same transaction, when `request.points_to_use > 0`, INSERT a `LoyaltyTransaction` row (`type = RESERVATION` when total>0 else `REDEMPTION`, `amount = -points_to_use`, `order_id`, `user_id`) and UPDATE `loyalty_accounts.balance` by subtracting `points_to_use` → satisfies "Loyalty reservation debits balance atomically" and the REDEMPTION branch of "Loyalty-only payment".
- [x] 3.5 [core-api] GREEN: Inside the same transaction, when `request.promocode_code` is truthy AND the `validate_promocode` return value is a `Promocode` model, INSERT a `PromocodeUsage` row (`promocode_id`, `user_id`, `order_id`) and UPDATE `promocodes.current_uses = current_uses + 1` for that row → satisfies "Promocode application increments usage counter".

## 4. `create_order` — post-commit side effects

- [x] 4.1 [core-api] GREEN: After the `with db_session.begin():` block exits successfully, when `total > 0`, call `enqueue_payment_task(order_id=order.id, amount=total, idempotency_key=idempotency_key)` exactly once and leave the Redis cart untouched → satisfies "Non-zero total enqueues Celery payment task after commit" AND "Non-zero total retains the Redis cart".
- [x] 4.2 [core-api] GREEN: After the `with db_session.begin():` block exits successfully, when `total == 0`, do NOT call `enqueue_payment_task`, and call `CartService(redis_client).delete(user_id)` — swallow any Redis exception with a debug-level log → satisfies "Loyalty-only payment (total = 0) … after commit the Redis `cart:{user_id}` key SHALL be deleted".
- [x] 4.3 [core-api] GREEN: Build and return an `OrderResponse` from the committed `Order` + `OrderItem[]` + `Payment` rows, including `id`, `status`, `type`, `items` (list of `OrderItemResponse`), `subtotal`, `discount_amount`, `points_used`, `delivery_fee`, `total`, `estimated_accrual`, `confirmation_url` (from the `Payment` row, may be `None`), `created_at` → satisfies the `OrderResponse` body shape assertions in the happy-path tests.

## 5. Orders HTTP router

- [x] 5.1 [core-api] GREEN: Create `services/core-api/src/core_api/routers/orders.py` with `orders_router = APIRouter(prefix="/api/v1/orders", tags=["orders"])`, imports of FastAPI, `CreateOrderRequest`, `OrderResponse`, `get_current_user`, `get_db`, `get_redis`, `create_order`, `EmptyCartError` → satisfies "POST /api/v1/orders creates an order" and "Orders router is registered in main.py" (file-exists and import-succeeds portions).
- [x] 5.2 [core-api] GREEN: Implement `POST /api/v1/orders` → `201 Created` endpoint that extracts `user_id` from `get_current_user`, calls `create_order(user_id, request, redis_client, db_session)`, catches `EmptyCartError` → raises `HTTPException(400, detail="Корзина пуста")`, catches `ValidatorError` (generic base for the validator stubs) → 409, lets Pydantic/422 bubble naturally → satisfies "Customer creates pickup order (happy path)", "Empty cart produces 400", "Validator failure produces 409", "Malformed body yields 422".
- [x] 5.3 [core-api] GREEN: Implement `GET /api/v1/orders/{order_id}` → `200 OK` endpoint that extracts `user_id`, runs `db_session.query(Order).filter(Order.id == order_id, Order.user_id == user_id).first()`, eager-loads `items` (`selectinload`) and joins the latest `Payment` for `confirmation_url`, returns 404 when no row → satisfies "Customer fetches own order detail", "Foreign order is 404", "Non-existent order id is 404".

## 6. Wire the router into the app

- [x] 6.1 [core-api] GREEN: Import `orders_router` in `services/core-api/src/core_api/main.py` and call `app.include_router(orders_router)` alongside the existing router registrations → satisfies OpenAPI exposes both operations.
- [x] 6.2 [core-api] GREEN: Add `("POST", "/api/v1/orders"): {Role.CUSTOMER}` and `("GET", "/api/v1/orders/{order_id}"): {Role.CUSTOMER}` to `ROUTE_MATRIX` in `services/core-api/src/core_api/rbac_matrix.py` — nothing added to `PUBLIC_ROUTES` → satisfies "Orders router is registered in main.py with RBAC matrix coverage", "Unauthenticated caller is rejected" (401), "Staff role is forbidden" (403 for POST and GET).

## 7. Verify

- [x] 7.1 [core-api] VERIFY: Run `TEST_DATABASE_URL=postgresql+psycopg://aura:aura@localhost:5432/aura_coffee_test .venv/bin/pytest services/core-api/tests/test_checkout_service.py services/core-api/tests/test_route_orders.py -q` from the worktree root and confirm all 60 tests pass (0 failed, 0 errored).
- [x] 7.2 [core-api] VERIFY: Run the full test suite (`.venv/bin/pytest services/core-api/tests -q`) and confirm no regressions vs. the pre-RED baseline — i.e., test_checkout_service.py and test_route_orders.py are all green and no other previously-passing test has regressed because of GREEN file additions or the `main.py` / `rbac_matrix.py` edits.
