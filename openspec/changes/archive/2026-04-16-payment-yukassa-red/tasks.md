## 1. Test scaffolding and dependencies

- [x] 1.1 [payment-worker] PREREQ: extend `services/payment-worker/pyproject.toml` `[project.optional-dependencies].dev` with `httpx>=0.27,<1.0`, `respx>=0.21,<1.0`, `fastapi>=0.111,<1.0`, `sqlalchemy>=2.0,<2.1`, `fakeredis>=2.20,<3.0`, and `pytest-asyncio>=0.23,<1.0`. No runtime deps added yet — production deps land in GREEN.
- [x] 1.2 [payment-worker] PREREQ: add `services/payment-worker/tests/__init__.py` (empty) so the `tests` package imports cleanly.
- [x] 1.3 [payment-worker] PREREQ: create `services/payment-worker/tests/conftest.py` with: (a) module-level SQLite engine at `sqlite:///./payment_worker_test.db` built from `shared.models.Base.metadata.create_all`, (b) `db_session` fixture yielding a `Session`, (c) `seed_user_order_payment` fixture that creates a User + Order (`status=CREATED`, `points_used=100`, `promocode_id` → a Promocode with `current_uses=1`) + Payment (`status=PENDING`) + matching RESERVATION loyalty row, (d) `fake_redis` fixture via `fakeredis.FakeRedis()`, (e) `test_env` fixture that `monkeypatch`es `YUKASSA_SHOP_ID`, `YUKASSA_SECRET_KEY`, `YUKASSA_WEBHOOK_IPS`, `DATABASE_URL=sqlite:///./payment_worker_test.db`, `REDIS_URL=redis://fake/0`.

## 2. RED: Settings tests

- [x] 2.1 [payment-worker] RED: add `services/payment-worker/tests/test_settings.py::test_settings_exposes_yukassa_and_db_fields` — imports `Settings` from `payment_worker.settings`, sets the five env vars, instantiates `Settings()`, and asserts `.yukassa_shop_id`, `.yukassa_secret_key`, `.yukassa_webhook_ips` (list of two CIDRs), `.database_url`, and `.redis_url` round-trip. MUST fail with `AttributeError` (existing Settings only has `redis_url`).
- [x] 2.2 [payment-worker] RED: add `test_yukassa_base_url_default` to `test_settings.py` — with `YUKASSA_BASE_URL` UNSET, asserts `Settings().yukassa_base_url == "https://api.yookassa.ru/v3"`. MUST fail with `AttributeError`.
- [x] 2.3 [payment-worker] RED: add `test_webhook_ips_parses_comma_list` to `test_settings.py` — with `YUKASSA_WEBHOOK_IPS="185.71.76.1, 185.71.76.2"`, asserts `Settings().yukassa_webhook_ips == ["185.71.76.1", "185.71.76.2"]` (whitespace trimmed). MUST fail.

## 3. RED: YukassaClient contract tests

- [x] 3.1 [payment-worker] RED: create `services/payment-worker/tests/test_yukassa_client.py::test_create_payment_posts_to_v3_payments` — uses `respx.mock()` to intercept `POST https://api.yookassa.ru/v3/payments`, calls `YukassaClient(shop_id="s", secret_key="k", base_url="https://api.yookassa.ru/v3").create_payment(amount_kopecks=12345, idempotency_key="uuid-1", return_url="https://r", description="Order #1")`, asserts URL, HTTP method, `Authorization: Basic ...` header equal to `base64("s:k")`, `Idempotency-Key: uuid-1` header, and JSON body `{"amount": {"value": "123.45", "currency": "RUB"}, "confirmation": {"type": "redirect", "return_url": "https://r"}, "capture": True, "description": "Order #1"}`. MUST fail with `ImportError` (no `yukassa_client` module).
- [x] 3.2 [payment-worker] RED: add `test_create_payment_returns_id_confirmation_status` to `test_yukassa_client.py` — `respx` returns `{"id": "pay_xyz", "status": "pending", "confirmation": {"confirmation_url": "https://yoo/confirm"}}`, asserts the method's return value exposes `payment_id == "pay_xyz"`, `confirmation_url == "https://yoo/confirm"`, `status == "pending"` (may be a dataclass, dict, or pydantic model — test uses dict-or-attr access helper). MUST fail.
- [x] 3.3 [payment-worker] RED: add `test_get_payment_hits_v3_payments_id_with_auth_no_idempotency` — `YukassaClient(...).get_payment("pay_abc")` intercepted by respx, asserts `GET /v3/payments/pay_abc`, HTTP Basic present, `Idempotency-Key` header ABSENT. MUST fail.
- [x] 3.4 [payment-worker] RED: add `test_create_refund_posts_full_refund` — `YukassaClient(...).create_refund(payment_id="pay_abc", amount_kopecks=12345, idempotency_key="uuid-2")` intercepted, asserts `POST /v3/refunds`, JSON body `{"payment_id": "pay_abc", "amount": {"value": "123.45", "currency": "RUB"}}`, `Idempotency-Key: uuid-2`. MUST fail.
- [x] 3.5 [payment-worker] RED: add `test_client_timeout_is_10_connect_30_read` — instantiates `YukassaClient(...)` and asserts the underlying `httpx.Client` or its stored timeout has `connect == 10.0` and `read == 30.0`. MUST fail.
- [x] 3.6 [payment-worker] RED: add `test_amount_formatting_edge_cases` — parametrized over `(100, "1.00")`, `(12345, "123.45")`, `(1, "0.01")`, `(100000, "1000.00")`; asserts that the JSON body posted to `/v3/payments` uses the formatted string. MUST fail.

## 4. RED: Celery task tests — create_payment

- [x] 4.1 [payment-worker] RED: create `services/payment-worker/tests/test_tasks.py::test_create_payment_happy_path_marks_awaiting_confirmation` — uses `seed_user_order_payment` fixture, patches `payment_worker.yukassa_client.YukassaClient` so that `create_payment` returns success, calls `create_payment.run(str(order.id), amount_kopecks=order.total, idempotency_key="uuid-3")`, asserts (after refresh) `Payment.status == PaymentStatus.AWAITING_CONFIRMATION`, `Payment.yukassa_payment_id == "pay_xyz"`, `Payment.confirmation_url == "https://yoo/confirm"`, and `Order.status == OrderStatus.CREATED`. MUST fail (task currently is `health_check`, not `create_payment`).
- [x] 4.2 [payment-worker] RED: add `test_create_payment_task_has_retry_policy` — asserts `create_payment.max_retries == 3`, `httpx.RequestError` in `create_payment.autoretry_for`, and `create_payment.retry_backoff` truthy. MUST fail with `AttributeError`.
- [x] 4.3 [payment-worker] RED: add `test_create_payment_compensation_on_exhausted_retries` — invokes the compensation helper directly (e.g. `payment_worker.tasks._fail_payment_and_cancel_order(session, payment_id)`), asserts `Payment.status == PAYMENT_FAILED`, `Order.status == CANCELLED`, a `LoyaltyTransaction` row with `type == LoyaltyTransactionType.REVERSAL` and `amount == +100` and `balance_after == seeded_balance + 100`, and `Promocode.current_uses == 0`. MUST fail with `AttributeError`.
- [x] 4.4 [payment-worker] RED: add `test_create_payment_compensation_is_atomic` — monkeypatches the promocode-update step to raise mid-transaction, asserts that neither the Payment nor the Order row was mutated (the whole compensation rolled back). MUST fail.

## 5. RED: Celery task tests — initiate_refund

- [x] 5.1 [payment-worker] RED: add `test_initiate_refund_success_marks_refund_pending` to `test_tasks.py` — given a Payment in `SUCCEEDED`, patches the YukassaClient to succeed, runs `initiate_refund.run(payment_id, amount_kopecks=12345)`, asserts `Payment.status == PaymentStatus.REFUND_PENDING`. MUST fail.
- [x] 5.2 [payment-worker] RED: add `test_initiate_refund_failure_does_not_mutate` — patches the YukassaClient to raise `httpx.RequestError("boom")`, runs the task, asserts the Payment row is unchanged (`SUCCEEDED`) and the task did NOT raise. MUST fail.

## 6. RED: Webhook endpoint tests — security

- [x] 6.1 [payment-worker] RED: create `services/payment-worker/tests/test_webhook.py::test_reject_request_from_untrusted_ip` — builds a `fastapi.testclient.TestClient(app)` against `payment_worker.webhook.app`, posts a valid `payment.succeeded` JSON body from `203.0.113.9` (simulated via `client.post("/webhooks/yukassa", json=..., headers={"X-Forwarded-For": "203.0.113.9"})`), asserts response `status_code == 403` and no Payment/Order mutations occurred. MUST fail with `ImportError`.
- [x] 6.2 [payment-worker] RED: add `test_duplicate_event_id_returns_200_without_mutation` — pre-marks `event_id="evt-dup"` in the idempotency store, posts a valid whitelisted webhook with the same event_id, asserts `200` and that `Payment.status` / `Order.status` are unchanged. MUST fail.

## 7. RED: Webhook endpoint tests — event dispatch

- [x] 7.1 [payment-worker] RED: add `test_payment_succeeded_advances_payment_and_order` to `test_webhook.py` — given seeded Payment (`AWAITING_CONFIRMATION`, `yukassa_payment_id="pay_xyz"`) + Order (`CREATED`) + RESERVATION loyalty row + Redis key `cart:{user_id}`, posts `{"event": "payment.succeeded", "object": {"id": "pay_xyz"}}`, asserts response 200, Payment → `SUCCEEDED`, Order → `PAID`, reserved loyalty converted to `REDEMPTION`, Redis key removed, and at least two Notification rows enqueued (channels `sms` + `in_app`, body contains `"Заказ оплачен"`). MUST fail.
- [x] 7.2 [payment-worker] RED: add `test_payment_canceled_cancels_order_and_unreserves` — given Payment (`AWAITING_CONFIRMATION`) + Order (`CREATED`, `points_used=50`, promo `current_uses=1`), posts `{"event": "payment.canceled", "object": {"id": "pay_xyz"}}`, asserts Payment → `PAYMENT_FAILED`, Order → `CANCELLED`, a `LoyaltyTransaction(type=REVERSAL, amount=+50)` exists, promocode `current_uses == 0`, and an `in_app` notification contains `"Платёж не прошёл"`. MUST fail.
- [x] 7.3 [payment-worker] RED: add `test_refund_succeeded_marks_refunded` — given Payment (`REFUND_PENDING`), posts `{"event": "refund.succeeded", "object": {"payment_id": "pay_xyz"}}`, asserts 200 and `Payment.status == REFUNDED`. MUST fail.
- [x] 7.4 [payment-worker] RED: add `test_refund_canceled_marks_refund_failed_and_notifies_admin` — given Payment (`REFUND_PENDING`), posts `{"event": "refund.canceled", ...}`, asserts `Payment.status == REFUND_FAILED` and at least one notification with `type == admin` (or `user_id == admin_id` — assertion shape documented in test). MUST fail.
- [x] 7.5 [payment-worker] RED: add `test_unknown_event_type_returns_200_no_mutation` — posts `{"event": "payment.waiting_for_capture", ...}`, asserts 200 and no state change. MUST fail.
- [x] 7.6 [payment-worker] RED: add `test_processing_exception_surfaces_as_500` — monkeypatches the dispatcher to raise `RuntimeError("kaboom")` for any event, posts a valid whitelisted `payment.succeeded`, asserts `response.status_code >= 500` (FastAPI default: 500) AND that `event_id` is NOT marked processed (so ЮKassa will retry). MUST fail.

## 8. VERIFY (RED)

- [x] 8.1 [payment-worker] VERIFY: run `pytest services/payment-worker/tests/ -v` (from container or host venv) and confirm every new test either FAILS (ImportError / AttributeError / AssertionError) or is legitimately skipped. No test should pass by accident.
- [x] 8.2 [payment-worker] VERIFY: confirm `health_check` task remains wired (`celery_app.tasks["payment_worker.tasks.health_check"]` — if still in `tasks.py`) OR that its test, if any, continues to pass. No pre-existing test regresses.
