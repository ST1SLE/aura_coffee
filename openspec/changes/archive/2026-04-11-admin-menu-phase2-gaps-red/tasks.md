## 1. Failing regression test for admin item-modifier set-replacement endpoint

- [x] 1.1 [core-api] RED: Add `test_admin_set_item_modifiers` to `services/core-api/tests/test_menu_admin.py`. The test MUST use the existing `client`, `admin_headers`, and `barista_headers` fixtures (no `db_client`, no `migrated_db_session`, no `_IS_SQLITE` skip — seed data through the admin API itself so the test is SQLite-safe). Flow: (a) seed one category via `POST /api/v1/admin/menu/categories` and one menu item in it via `POST /api/v1/admin/menu/items`, capturing `item_id`; (b) seed three modifiers via `POST /api/v1/admin/menu/modifiers`, capturing `mod_a`, `mod_b`, `mod_c`; (c) assert `PUT /api/v1/admin/menu/items/{item_id}/modifiers` with `admin_headers` and body `{"modifier_ids": [mod_a, mod_b, mod_c]}` → 200 and `set(r["modifiers"][i]["id"] for i) == {mod_a, mod_b, mod_c}`; (d) assert the same PUT with body `{"modifier_ids": [mod_b]}` → 200 and the returned `modifiers` array contains exactly one entry with id `mod_b` (replacement semantics, not union); (e) assert the same PUT with body `{"modifier_ids": []}` → 200 and `r["modifiers"] == []`; (f) assert the same PUT with body `{"modifier_ids": [mod_a, 999999]}` → 422, response `detail` names `999999` as the offending id, AND a follow-up `GET /api/v1/admin/menu/items/{item_id}` shows the modifier set unchanged from before the 422 call; (g) assert `PUT /api/v1/admin/menu/items/999999/modifiers` with body `{"modifier_ids": [mod_a]}` → 404; (h) assert the same PUT with body `{"modifier_ids": [mod_a, mod_a, mod_b]}` → 200 and the returned `modifiers` array contains exactly `{mod_a, mod_b}` (dedup); (i) assert the same PUT with `barista_headers` → 403 regardless of body; (j) assert the same PUT with NO `Authorization` header → 401. Expected red state against current `main`: every sub-assertion starting at (c) fails because the route does not exist — FastAPI returns 405 (Method Not Allowed) or 404 for the path, never 200. The failure on (c) is the first visible RED signal.
