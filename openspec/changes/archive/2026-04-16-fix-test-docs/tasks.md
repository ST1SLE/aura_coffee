## 1. Fix Block 1.3 — size-creation field name

- [x] 1.1 IMPL: [docs] Replace `"item_id":` with `"menu_item_id":` in all 3 size-creation curl payloads (lines 135-137 of `docs/phase2_manual_test_scenarios.md`)

## 2. Fix Block 3.6 — decrement-to-remove expectation

- [x] 2.1 IMPL: [docs] Rewrite Block 3.6 heading and body to reflect DELETE-based remove behavior when qty=1 (`docs/phase2_manual_test_scenarios.md`)

## 3. Fix Block 3.8 — remove Clear cart hedge

- [x] 3.1 IMPL: [docs] Remove "(if button exists)" from Block 3.8 Clear cart instruction (`docs/phase2_manual_test_scenarios.md`)

## 4. Verify

- [x] 4.1 VERIFY: [docs] Grep file for any remaining `"item_id"` occurrences and proofread changed blocks for consistency
