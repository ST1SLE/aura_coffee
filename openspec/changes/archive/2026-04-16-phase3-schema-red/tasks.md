## 1. Test module scaffolding

- [x] 1.1 [core-api] PREREQ: create empty test module `services/core-api/tests/test_models_order.py` with module docstring describing the RED contract (covers 9 new SQLAlchemy models from PDD §5.2) and the two imports that every test file needs (`pytest`, `sqlalchemy as sa`, `from sqlalchemy import inspect as sa_inspect`, and `from tests.conftest import _TEST_DB_URL as TEST_DB_URL` + module-level `_IS_SQLITE = TEST_DB_URL.startswith("sqlite")`).
- [x] 1.2 [core-api] PREREQ: create empty test module `services/core-api/tests/test_schemas_order.py` with module docstring, `pytest` and `pydantic.BaseModel, ValidationError` imports, and no top-level imports from `core_api.schemas.order` (target lives inside each test).
- [x] 1.3 [core-api] PREREQ: create empty test module `services/core-api/tests/test_migration_0005_phase3_schema.py` with module docstring, imports (`os`, `pytest`, `alembic.command`, `alembic.config.Config`, `sqlalchemy.create_engine, inspect, text`, `from tests.conftest import _TEST_DB_URL as TEST_DB_URL`), the `ALEMBIC_INI` constant, the `_is_sqlite` helper, the module-scoped `alembic_cfg` fixture (mirrors `test_migration_0004_menu_tables.py`), and the module-scoped `migrated_engine` fixture that runs `alembic upgrade head` and on teardown runs `alembic downgrade 0004`.

## 2. RED: enum tests in test_models_order.py

- [x] 2.1 [core-api] RED: add `test_order_status_enum_exists_with_correct_values` to `test_models_order.py` — imports `OrderStatus` inside the body, asserts `{e.value for e in OrderStatus} == {"created", "paid", "preparing", "ready", "in_delivery", "completed", "cancelled"}`, asserts `issubclass(OrderStatus, str)`. MUST fail in RED with `ImportError`.
- [x] 2.2 [core-api] RED: add `test_order_type_enum_exists_with_correct_values` — asserts `OrderType.PICKUP.value == "pickup"`, `OrderType.DELIVERY.value == "delivery"`, enum has exactly 2 members. MUST fail in RED with `ImportError`.
- [x] 2.3 [core-api] RED: add `test_payment_status_enum_exists_with_correct_values` — asserts `{e.value for e in PaymentStatus} == {"pending", "awaiting_confirmation", "succeeded", "payment_failed", "refund_pending", "refunded", "refund_failed"}`. MUST fail in RED.
- [x] 2.4 [core-api] RED: add `test_refund_status_enum_exists_with_correct_values` — asserts `{e.value for e in RefundStatus} == {"pending", "succeeded", "failed"}`. MUST fail in RED.
- [x] 2.5 [core-api] RED: add `test_notification_channel_enum_exists_with_correct_values` — asserts `{e.value for e in NotificationChannel} == {"in_app", "sms"}`. MUST fail in RED.
- [x] 2.6 [core-api] RED: add `test_notification_type_enum_exists_with_correct_values` — asserts `{e.value for e in NotificationType} == {"order_status_change", "otp"}`. MUST fail in RED.
- [x] 2.7 [core-api] RED: add `test_notification_status_enum_exists_with_correct_values` — asserts `{e.value for e in NotificationStatus} == {"pending", "sent", "failed"}`. MUST fail in RED.
- [x] 2.8 [core-api] RED: add `test_loyalty_transaction_type_enum_exists_with_correct_values` — asserts `{e.value for e in LoyaltyTransactionType} == {"accrual", "redemption", "reversal", "reservation", "admin_adjustment"}`. MUST fail in RED.
- [x] 2.9 [core-api] RED: add `test_promocode_discount_type_enum_exists_with_correct_values` — asserts `{e.value for e in PromocodeDiscountType} == {"percent", "fixed_amount"}`. MUST fail in RED.

## 3. RED: ORM-mapper tests for order/order_item tables

- [x] 3.1 [core-api] RED: add `test_shop_settings_model_declares_columns` — inspects `ShopSettings`, asserts `__tablename__ == "shop_settings"`, columns superset `{"id", "shop_lat", "shop_lon", "delivery_radius_km", "min_delivery_amount", "free_delivery_threshold", "delivery_fee", "loyalty_percent", "default_prep_time_minutes", "estimated_delivery_time_minutes", "working_hours", "updated_at"}`, `id` column type is an Integer variant (not UUID). MUST fail with `ImportError`.
- [x] 3.2 [core-api] RED: add `test_order_model_declares_columns` — inspects `Order`, asserts tablename `"orders"`, columns superset covers every PDD §5.2 field (`id`, `user_id`, `status`, `type`, `requested_time`, `estimated_ready_at`, `delivery_address_snapshot`, `subtotal`, `discount_amount`, `points_used`, `delivery_fee`, `total`, `estimated_accrual`, `promocode_id`, `cancelled_by`, `cancelled_at`, `auto_completed`, `auto_completed_at`, `created_at`, `updated_at`), and that `users` appears among FK referred tables. MUST fail with `ImportError`.
- [x] 3.3 [core-api] RED: add `test_order_user_fk_and_promocode_fk` — inspects `Order`'s FK set, asserts there is a FK on `user_id → users.id` and a nullable FK on `promocode_id → promocodes.id`. MUST fail.
- [x] 3.4 [core-api] RED: add `test_order_model_forbids_pii_columns` — asserts column names of `Order` do NOT contain any of `phone`, `phone_hash`, `customer_phone`, `customer_name`, `address_text` (INV-013). Passes vacuously if the model is absent — make the assertion only after a successful import; if import fails the test fails on that. MUST fail in RED with `ImportError`.
- [x] 3.5 [core-api] RED: add `test_order_item_model_declares_columns` — inspects `OrderItem`, asserts tablename `"order_items"`, columns superset `{"id", "order_id", "menu_item_id", "menu_item_name_ru", "menu_item_name_en", "size_option_id", "size_label", "unit_price", "modifiers_snapshot", "quantity", "line_total"}`, and that `orders` is in the FK referred tables. MUST fail.
- [x] 3.6 [core-api] RED: add `test_order_item_order_fk_cascade` — inspects `OrderItem.order_id` FK, asserts `ondelete == "CASCADE"`. MUST fail.
- [x] 3.7 [core-api] RED: add `test_order_item_menu_item_id_is_not_fk` — inspects the `OrderItem` mapper, asserts `menu_item_id` has NO `ForeignKey` constraint and `size_option_id` has NO `ForeignKey` constraint (§7.7: repeat order lookup, INV-014: snapshot). MUST fail.
- [x] 3.8 [core-api] RED: add `test_order_item_nullable_snapshot_references` — asserts `menu_item_id.nullable is True` and `size_option_id.nullable is True` (reference may be NULL if the lookup is stale), while `menu_item_name_ru.nullable is False`, `menu_item_name_en.nullable is False`, `unit_price.nullable is False`, `line_total.nullable is False`. MUST fail.

## 4. RED: ORM-mapper tests for payment/refund/loyalty/promocode/notification

- [x] 4.1 [core-api] RED: add `test_payment_model_declares_columns` — tablename `"payments"`, columns `{"id", "order_id", "yukassa_payment_id", "amount", "status", "confirmation_url", "idempotency_key", "created_at", "updated_at"}`. MUST fail.
- [x] 4.2 [core-api] RED: add `test_payment_order_fk_is_unique` — inspects `Payment.order_id` FK target is `orders.id` and there is a unique constraint (or unique index) on `order_id` (1:1 with orders, PDD §5.2). MUST fail.
- [x] 4.3 [core-api] RED: add `test_refund_model_declares_columns` — tablename `"refunds"`, columns `{"id", "payment_id", "yukassa_refund_id", "amount", "status", "reason", "created_at"}`. MUST fail.
- [x] 4.4 [core-api] RED: add `test_refund_payment_fk` — inspects `Refund.payment_id` FK target is `payments.id`. MUST fail.
- [x] 4.5 [core-api] RED: add `test_loyalty_transaction_model_declares_columns` — tablename `"loyalty_transactions"`, columns `{"id", "user_id", "order_id", "type", "amount", "balance_after", "description", "created_at"}`, `order_id.nullable is True`. MUST fail.
- [x] 4.6 [core-api] RED: add `test_promocode_model_declares_columns` — tablename `"promocodes"`, columns `{"id", "code", "discount_type", "discount_value", "min_order_amount", "valid_from", "valid_until", "max_uses", "max_uses_per_user", "current_uses", "is_active", "created_at"}`. MUST fail.
- [x] 4.7 [core-api] RED: add `test_promocode_usage_model_declares_columns` — tablename `"promocode_usages"`, columns `{"id", "promocode_id", "user_id", "order_id", "created_at"}`; FKs point to `promocodes.id`, `users.id`, `orders.id`. MUST fail.
- [x] 4.8 [core-api] RED: add `test_notification_model_declares_columns` — tablename `"notifications"`, columns `{"id", "user_id", "order_id", "channel", "type", "message_ru", "message_en", "status", "sent_at", "created_at"}`, `order_id.nullable is True`, `sent_at.nullable is True`. MUST fail.

## 5. RED: shared.models __all__ registration test

- [x] 5.1 [shared] RED: add `test_phase3_models_registered_in_shared_models_all` (in `test_models_order.py`) — imports `shared.models` and asserts `"ShopSettings" in shared.models.__all__` AND likewise for every other Phase-3 model name (`"Order"`, `"OrderItem"`, `"Payment"`, `"Refund"`, `"LoyaltyTransaction"`, `"Promocode"`, `"PromocodeUsage"`, `"Notification"`). MUST fail in RED.

## 6. RED: group-wide REFACTOR marker (none required)

- [x] 6.1 [core-api] PREREQ: record that this RED change ships tests only — no GREEN or REFACTOR belongs to this change. GREEN/REFACTOR work lives in the sibling change `phase3-schema-green`.

## 7. RED: Pydantic schema tests (test_schemas_order.py)

- [x] 7.1 [core-api] RED: add `test_order_schemas_exist` — imports `CreateOrderRequest`, `OrderItemResponse`, `OrderResponse`, `OrderListResponse`, `OrderStatusUpdate`, `CancelOrderRequest`, `RepeatOrderResult` from `core_api.schemas.order`, asserts each is a `BaseModel` subclass. MUST fail with `ImportError`.
- [x] 7.2 [core-api] RED: add `test_shop_settings_schema_exists` — imports `ShopSettingsResponse` from `core_api.schemas.shop_settings`, asserts it is a `BaseModel` subclass. MUST fail with `ImportError`.
- [x] 7.3 [core-api] RED: add `test_create_order_request_accepts_minimal_pickup` — constructs `CreateOrderRequest(type="pickup")`, asserts `.type == OrderType.PICKUP` (or equivalent enum coercion), `.points_to_use == 0`, `.delivery_address is None`, `.requested_time is None`, `.promocode_code is None`. MUST fail with `ImportError`.
- [x] 7.4 [core-api] RED: add `test_create_order_request_rejects_unknown_type` — asserts `pytest.raises(ValidationError)` when `CreateOrderRequest(type="takeaway")`. MUST fail.
- [x] 7.5 [core-api] RED: add `test_create_order_request_accepts_delivery_with_address` — constructs a payload with `type="delivery"` and a `delivery_address` object with `text`, `lat`, `lon`, `apartment`, `entrance`, `floor`, `comment`; asserts fields round-trip. MUST fail.
- [x] 7.6 [core-api] RED: add `test_create_order_request_rejects_negative_points_to_use` — asserts `ValidationError` when `points_to_use=-1`. MUST fail.
- [x] 7.7 [core-api] RED: add `test_order_item_response_round_trip_from_namespace` — constructs a `SimpleNamespace` with the order-item fields from the task brief and asserts `OrderItemResponse.model_validate(ns)` round-trips `menu_item_name_ru`, `menu_item_name_en`, `size_label`, `unit_price`, `modifiers_snapshot`, `quantity`, `line_total`. MUST fail.
- [x] 7.8 [core-api] RED: add `test_order_response_round_trip_from_namespace` — builds a `SimpleNamespace` with all required fields (`id`, `status`, `type`, `items=[]`, `subtotal`, `discount_amount`, `points_used`, `delivery_fee`, `total`, `estimated_accrual`, `confirmation_url=None`, `requested_time=None`, `estimated_ready_at=None`, `cancelled_by=None`, `cancelled_at=None`, `created_at`) and asserts `OrderResponse.model_validate(ns)` succeeds. MUST fail.
- [x] 7.9 [core-api] RED: add `test_order_list_response_schema` — constructs `OrderListResponse(items=[], total_count=0, page=1, per_page=20)`, asserts attributes round-trip. MUST fail.
- [x] 7.10 [core-api] RED: add `test_order_status_update_schema` — constructs `OrderStatusUpdate(new_status="preparing")`, asserts `.new_status == OrderStatus.PREPARING`. MUST fail.
- [x] 7.11 [core-api] RED: add `test_cancel_order_request_schema` — constructs `CancelOrderRequest()` (no args) and `CancelOrderRequest(reason="клиент передумал")`, asserts optional `.reason` defaults to `None`. MUST fail.
- [x] 7.12 [core-api] RED: add `test_repeat_order_result_schema` — constructs `RepeatOrderResult(added_to_cart=3, skipped=[{"name": "Латте", "reason": "stop_list"}])`, asserts the `.added_to_cart` and `.skipped` shape. MUST fail.
- [x] 7.13 [core-api] RED: add `test_shop_settings_response_round_trip` — builds `SimpleNamespace` with canonical defaults and asserts `ShopSettingsResponse.model_validate(ns)` exposes every field. MUST fail.

## 8. RED: migration tests (test_migration_0005_phase3_schema.py)

- [x] 8.1 [core-api] RED: add `test_upgrade_creates_shop_settings_table` — runs `migrated_engine`, asserts `shop_settings` is in `inspect(engine).get_table_names()`, columns cover every expected field, `id` column type reflects as integer, `working_hours` column type name reflects as `JSONB`. Skipped on SQLite. MUST fail.
- [x] 8.2 [core-api] RED: add `test_shop_settings_check_id_is_1` — against `migrated_engine`, attempts `INSERT INTO shop_settings (id, loyalty_percent, delivery_fee, min_delivery_amount, free_delivery_threshold, default_prep_time_minutes, estimated_delivery_time_minutes, shop_lat, shop_lon, delivery_radius_km, working_hours) VALUES (2, ...)`, expects an integrity/DB error, verifies the CHECK constraint named `ck_shop_settings_singleton` (or similar) exists via pg_constraint. Skipped on SQLite. MUST fail.
- [x] 8.3 [core-api] RED: add `test_upgrade_creates_orders_table` — asserts `orders` table exists, required columns present, `status` is a PG enum with the 7 labels (read via `pg_type` join `pg_enum`), `type` is a PG enum with 2 labels, `delivery_address_snapshot` reflects as `JSONB`, `requested_time` and `estimated_ready_at` are TIMESTAMPTZ (nullable). Skipped on SQLite. MUST fail.
- [x] 8.4 [core-api] RED: add `test_orders_user_fk_behavior` — FK `orders.user_id → users.id` exists. Skipped on SQLite. MUST fail.
- [x] 8.5 [core-api] RED: add `test_upgrade_creates_order_items_table` — asserts `order_items` table exists, required columns present, `modifiers_snapshot` reflects as `JSONB`, the `order_id` FK has `ondelete = CASCADE`, and `menu_item_id`/`size_option_id` columns exist with NO foreign-key constraints. Skipped on SQLite. MUST fail.
- [x] 8.6 [core-api] RED: add `test_upgrade_creates_payments_table_and_order_unique` — asserts `payments` table exists, columns present, the FK on `payments.order_id → orders.id` is backed by a UNIQUE constraint or UNIQUE index on `order_id`. Skipped on SQLite. MUST fail.
- [x] 8.7 [core-api] RED: add `test_upgrade_creates_refunds_table` — tablename, columns, FK `refunds.payment_id → payments.id`, `status` is PG enum `refund_status` with 3 labels. Skipped on SQLite. MUST fail.
- [x] 8.8 [core-api] RED: add `test_upgrade_creates_loyalty_transactions_table` — tablename, columns, FKs, `type` is PG enum `loyalty_transaction_type` with 5 labels. Skipped on SQLite. MUST fail.
- [x] 8.9 [core-api] RED: add `test_upgrade_creates_promocodes_table` — tablename, columns, `code` has UNIQUE constraint/index, `discount_type` is PG enum `promocode_discount_type` with 2 labels. Skipped on SQLite. MUST fail.
- [x] 8.10 [core-api] RED: add `test_upgrade_creates_promocode_usages_table` — tablename, columns, three FKs (`promocode_id`, `user_id`, `order_id`). Skipped on SQLite. MUST fail.
- [x] 8.11 [core-api] RED: add `test_upgrade_creates_notifications_table` — tablename, columns, FKs, three PG enums (`notification_channel`, `notification_type`, `notification_status`). Skipped on SQLite. MUST fail.
- [x] 8.12 [core-api] RED: add `test_upgrade_creates_phase3_indexes` — asserts every PDD §5.4 Phase-3 index exists by name or column set: `ix_orders_user_created_at` on `orders(user_id, created_at)`, a partial index on `orders(status)` whose `postgresql_where` predicate references `completed` and `cancelled`, `ix_orders_type_status` on `orders(type, status)`, UNIQUE index on `promocodes(code)`, `ix_promocode_usages_promocode_user` on `promocode_usages(promocode_id, user_id)`, `ix_loyalty_transactions_user_created_at` on `loyalty_transactions(user_id, created_at)`, `ix_notifications_user_created_at` on `notifications(user_id, created_at)`. Skipped on SQLite. MUST fail.
- [x] 8.13 [core-api] RED: add `test_shop_settings_seed_populates_defaults` — runs migration, invokes `from database.seeds.shop_settings import run; run(TEST_DB_URL)`, queries the single row and asserts `(id, shop_lat, shop_lon, delivery_radius_km, min_delivery_amount, free_delivery_threshold, delivery_fee, loyalty_percent, default_prep_time_minutes, estimated_delivery_time_minutes) == (1, 55.7558, 37.6173, 5, 50000, 150000, 20000, 5, 15, 30)` and `working_hours` JSONB has keys `{"mon","tue","wed","thu","fri","sat","sun"}` each with `{"open":"08:00","close":"22:00"}`. Skipped on SQLite. MUST fail.
- [x] 8.14 [core-api] RED: add `test_shop_settings_seed_is_idempotent` — calls `run(TEST_DB_URL)` twice, asserts exactly one row remains and values match the defaults. Skipped on SQLite. MUST fail.
- [x] 8.15 [core-api] RED: add `test_downgrade_removes_phase3_tables_and_enums` — runs upgrade head, then `alembic downgrade 0004`, asserts none of the 9 Phase-3 tables exist and none of the 9 Phase-3 PG enums remain in `pg_type`. Uses its own engine (does not rely on the module-scoped `migrated_engine`). Skipped on SQLite. MUST fail.

## 9. VERIFY (RED)

- [x] 9.1 [core-api] VERIFY: run `pytest services/core-api/tests/test_models_order.py services/core-api/tests/test_schemas_order.py services/core-api/tests/test_migration_0005_phase3_schema.py -v` against the existing codebase; confirm that every new test either FAILS or is legitimately skipped (SQLite skip). No test should pass by accident.
- [x] 9.2 [core-api] VERIFY: run `pytest services/core-api/tests/` (the full suite) and confirm that existing Phase-1/Phase-2 tests still pass — the new files must not regress collection or shared fixtures.
