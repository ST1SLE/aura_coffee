## Tasks

### 1. Guard mount refresh with useRef

- **1.1** [web-customer] Add `useRef` guard to refresh `useEffect` in `web/customer/src/auth/AuthProvider.tsx` — prevent duplicate refresh call under StrictMode double-mount
