## 1. Test file — Delivery Assignment state machine

- [x] 1.1 [core-api] RED: create `services/core-api/tests/test_delivery_assignment_state_machine.py` with an import probe `test_delivery_assignment_module_exists` that does `from core_api.services.delivery_assignment import take_assignment, pickup_assignment, deliver_assignment, cancel_assignment_for_order, list_available_for_courier, AssignmentTransitionError, AssignmentAlreadyTakenError`; MUST fail with `ModuleNotFoundError` until GREEN.
- [x] 1.2 [core-api] RED: add `test_take_assignment_awaiting_to_assigned_happy_path` — seeds `DeliveryAssignment(status=AWAITING_COURIER)` + `StaffAccount(role=COURIER)`, calls `take_assignment(aid, cid, db)`, asserts `status == COURIER_ASSIGNED`, `courier_id == cid`, `assigned_at is not None`.
- [x] 1.3 [core-api] RED: add `test_pickup_assignment_assigned_to_picked_up_happy_path` — seeds `Order(status=READY, type=DELIVERY)` + `DeliveryAssignment(status=COURIER_ASSIGNED, courier_id=cid)`, patches `core_api.services.delivery_assignment.transition_order_bridge` to a no-op that still updates order.status to IN_DELIVERY, calls `pickup_assignment(aid, cid, db)`, asserts `assignment.status == PICKED_UP` and `picked_up_at is not None`.
- [x] 1.4 [core-api] RED: add `test_deliver_assignment_picked_up_to_delivered_happy_path` — same shape, seeds PICKED_UP assignment + IN_DELIVERY order, patches `transition_order_bridge`, calls `deliver_assignment(aid, cid, db)`, asserts `status == DELIVERED` and `delivered_at is not None`.
- [x] 1.5 [core-api] RED: add `test_cancel_assignment_for_order_awaiting_to_cancelled` — seeds assignment `AWAITING_COURIER`, calls `cancel_assignment_for_order(order_id, db)`, asserts `status == CANCELLED` and `cancelled_at is not None`; returns the DeliveryAssignment object (not None).
- [x] 1.6 [core-api] RED: add `test_cancel_assignment_for_order_assigned_to_cancelled` — seeds `COURIER_ASSIGNED`, calls cancel, asserts CANCELLED.
- [x] 1.7 [core-api] RED: add `test_cancel_assignment_for_order_picked_up_is_noop` — seeds `PICKED_UP`, calls cancel, asserts `status == PICKED_UP` unchanged and return value is `None`.
- [x] 1.8 [core-api] RED: add `test_cancel_assignment_for_order_when_no_assignment_returns_none` — seeds an order with NO assignment row, calls `cancel_assignment_for_order(order_id, db)`, asserts return value is `None` and no exception.
- [x] 1.9 [core-api] RED: add `test_take_assignment_on_non_awaiting_raises_forbidden_transition` — parametrised over `COURIER_ASSIGNED`, `PICKED_UP`, `DELIVERED`, `CANCELLED`; each case raises `AssignmentTransitionError(reason="forbidden_transition")`.
- [x] 1.10 [core-api] RED: add parametrised `test_forbidden_pickup_transitions` — assignment not in `COURIER_ASSIGNED` (`AWAITING_COURIER`, `PICKED_UP`, `DELIVERED`, `CANCELLED`) → `pickup_assignment` raises `forbidden_transition`; seed ownership so it isn't a `not_owner` false positive.
- [x] 1.11 [core-api] RED: add parametrised `test_forbidden_deliver_transitions` — assignment not in `PICKED_UP` (all other 4 states) → `deliver_assignment` raises `forbidden_transition`.
- [x] 1.12 [core-api] RED: add `test_pickup_ownership_mismatch_raises_not_owner` — assignment `COURIER_ASSIGNED` with `courier_id=c1`; `pickup_assignment(aid, c2, db)` raises `AssignmentTransitionError(reason="not_owner")`; DB unchanged.
- [x] 1.13 [core-api] RED: add `test_deliver_ownership_mismatch_raises_not_owner` — assignment `PICKED_UP` with `courier_id=c1`; `deliver_assignment(aid, c2, db)` raises `AssignmentTransitionError(reason="not_owner")`.
- [x] 1.14 [core-api] RED: add `test_pickup_requires_order_ready` — assignment `COURIER_ASSIGNED`, but linked order `status` ≠ READY (parametrise over `PAID`, `PREPARING`, `IN_DELIVERY`); `pickup_assignment` raises `AssignmentTransitionError(reason="order_not_ready")`; DB unchanged.
- [x] 1.15 [core-api] RED: add `test_assignment_not_found_raises` — `take_assignment(uuid4(), cid, db)` on a non-existent id raises `AssignmentTransitionError(reason="assignment_not_found")`.
- [x] 1.16 [core-api] RED: add `test_list_available_for_courier_returns_only_awaiting` — seeds 3 assignments (AWAITING, COURIER_ASSIGNED, DELIVERED), each on a distinct order; `list_available_for_courier(db)` returns a list of length 1 whose single element's `id` matches the AWAITING row; element also exposes `order_id`, `total`, `delivery_address_snapshot`, `requested_time` (join with orders).

## 2. Test file — Bridge to Order Lifecycle (§6.1 ↔ §6.3)

- [x] 2.1 [core-api] RED: create `services/core-api/tests/test_delivery_assignment_bridge.py` with an import probe `test_transition_order_bridge_exists` that does `from core_api.services.order_lifecycle import transition_order_bridge`; MUST fail with `ImportError` until GREEN.
- [x] 2.2 [core-api] RED: add `test_paid_to_preparing_delivery_creates_awaiting_assignment` — seeds `Order(status=PAID, type=DELIVERY)` + `LoyaltyAccount`; patches notifications; calls `transition_order(order.id, PREPARING, "barista", db)`; asserts exactly one row in `delivery_assignments` with `order_id == order.id`, `status == AWAITING_COURIER`, `courier_id IS NULL`.
- [x] 2.3 [core-api] RED: add `test_paid_to_preparing_pickup_does_not_create_assignment` — same shape but `type=PICKUP`; asserts zero `delivery_assignments` rows for that order.
- [x] 2.4 [core-api] RED: add `test_pickup_assignment_transitions_order_to_in_delivery` — seeds Order(READY, DELIVERY) + assignment(COURIER_ASSIGNED, courier=c1); patches notifications; DOES NOT patch `transition_order_bridge` — exercises the real bridge. Calls `pickup_assignment(aid, c1, db)`. Asserts `order.status == IN_DELIVERY` AND `assignment.status == PICKED_UP` in the same session.
- [x] 2.5 [core-api] RED: add `test_deliver_assignment_transitions_order_to_completed_and_accrues_loyalty` — seeds Order(IN_DELIVERY, DELIVERY, total=100000, delivery_fee=15000), assignment(PICKED_UP, courier=c1), ShopSettings(loyalty_percent=10), LoyaltyAccount(balance=0); patches notifications; calls `deliver_assignment(aid, c1, db)`; asserts `order.status == COMPLETED`, `assignment.status == DELIVERED`, `LoyaltyAccount.balance == 8500`, exactly one `LoyaltyTransaction(type=ACCRUAL, amount=8500)` exists.
- [x] 2.6 [core-api] RED: add `test_pickup_rolls_back_if_order_transition_fails` — monkey-patches `core_api.services.delivery_assignment.transition_order_bridge` to `raise RuntimeError("boom")`; before the call commits the pre-state with `db.commit()`. Calls `pickup_assignment` inside a fresh session; expects `RuntimeError` to propagate. After `db.rollback()` + `db.expire_all()`, asserts `assignment.status == COURIER_ASSIGNED` (unchanged) and `order.status == READY` (unchanged).
- [x] 2.7 [core-api] RED: add `test_deliver_rolls_back_if_accrual_fails` — monkey-patches `core_api.services.order_lifecycle._accrue_loyalty` to raise; asserts rollback of assignment (`PICKED_UP` not `DELIVERED`), order (`IN_DELIVERY` not `COMPLETED`), no ACCRUAL row.
- [x] 2.8 [core-api] RED: add `test_cancel_assignment_for_order_integrates_with_order_cancel` — drives the chain via real `order_cancel.cancel_order(order_id, "admin", reason, db)`: seeds Order(PAID, DELIVERY), transitions to PREPARING (so assignment is created), then admin cancels. Asserts `order.status == CANCELLED` AND `assignment.status == CANCELLED` in the same session. (This test may fail with AttributeError in RED because `order_cancel` does not yet call `cancel_assignment_for_order` — the AttributeError / wrong state is a valid red state.)

## 3. Test file — Courier HTTP router & RBAC

- [x] 3.1 [core-api] RED: create `services/core-api/tests/test_courier_endpoints.py` with an import probe `test_courier_router_module_exists` that does `from core_api.routers.courier import router`; MUST fail with `ModuleNotFoundError` until GREEN.
- [x] 3.2 [core-api] RED: add `test_main_registers_courier_router` — imports `core_api.main.app`, collects `app.router.routes`, asserts that `("GET", "/api/v1/courier/assignments/available")`, `("GET", "/api/v1/courier/assignments/mine")`, `("POST", "/api/v1/courier/assignments/{assignment_id}/take")`, `("POST", "/api/v1/courier/assignments/{assignment_id}/pickup")`, `("POST", "/api/v1/courier/assignments/{assignment_id}/deliver")` are all present.
- [x] 3.3 [core-api] RED: add `test_rbac_matrix_has_courier_entries` — imports `core_api.rbac_matrix.ROUTE_MATRIX`, asserts each of the 5 paths maps to `{COURIER}` exactly (no admin, no barista).
- [x] 3.4 [core-api] RED: add parametrised `test_available_endpoint_rejects_non_courier_with_403` — customer / barista / admin headers → 403 on GET `/api/v1/courier/assignments/available`.
- [x] 3.5 [core-api] RED: add parametrised `test_mine_endpoint_rejects_non_courier_with_403` — same, GET `/mine`.
- [x] 3.6 [core-api] RED: add parametrised `test_take_endpoint_rejects_non_courier_with_403` — POST `/{id}/take`, bodies empty.
- [x] 3.7 [core-api] RED: add parametrised `test_pickup_endpoint_rejects_non_courier_with_403` — POST `/{id}/pickup`.
- [x] 3.8 [core-api] RED: add parametrised `test_deliver_endpoint_rejects_non_courier_with_403` — POST `/{id}/deliver`.
- [x] 3.9 [core-api] RED: add `test_take_endpoint_forwards_already_taken_as_409` — monkey-patches `core_api.routers.courier.take_assignment` to raise `AssignmentAlreadyTakenError`; courier_headers call; asserts 409 and body contains `reason == "already_taken"`.
- [x] 3.10 [core-api] RED: add `test_pickup_endpoint_forwards_not_owner_as_403` — patched `pickup_assignment` raises `AssignmentTransitionError(reason="not_owner")`; HTTP 403 with reason in body.
- [x] 3.11 [core-api] RED: add `test_deliver_endpoint_forwards_forbidden_transition_as_409` — patched `deliver_assignment` raises `AssignmentTransitionError(reason="forbidden_transition")`; HTTP 409.
- [x] 3.12 [core-api] RED: add `test_assignment_not_found_returns_404` — patched service raises `AssignmentTransitionError(reason="assignment_not_found")`; any courier endpoint returns 404 with reason in body.
- [x] 3.13 [core-api] RED: add `test_available_endpoint_returns_json_list` — patched `list_available_for_courier` returns a stub list of length 1; courier GET returns 200 + JSON array of length 1; asserts top-level JSON is a list (not wrapped in `{items: [...]}`).

## 4. Test file — Optimistic lock race

- [x] 4.1 [core-api] RED: create `services/core-api/tests/test_delivery_assignment_optimistic_lock.py` with an import probe `test_optimistic_lock_module_exists` that does `from core_api.services.delivery_assignment import take_assignment, AssignmentAlreadyTakenError`; MUST fail until GREEN.
- [x] 4.2 [core-api] RED: add `test_two_couriers_race_one_wins` — `pytest.skip(...)` if `TEST_DATABASE_URL.startswith("sqlite")`; otherwise opens two independent `Session(engine)` instances on the PG engine from `migrated_db_session`, seeds assignment `AWAITING_COURIER` in a third session, commits; calls `take_assignment(aid, c1, db_A)` in session A and commits; then calls `take_assignment(aid, c2, db_B)` in session B and asserts `AssignmentAlreadyTakenError`. Then opens a fresh session C, re-reads the row, asserts `courier_id == c1`, `status == COURIER_ASSIGNED`.

## 5. Verify RED

- [x] 5.1 [core-api] VERIFY: run `pytest services/core-api/tests/test_delivery_assignment_state_machine.py services/core-api/tests/test_delivery_assignment_bridge.py services/core-api/tests/test_courier_endpoints.py services/core-api/tests/test_delivery_assignment_optimistic_lock.py -v` inside the core-api container; confirm every new test fails (ModuleNotFoundError / ImportError / AttributeError / 404 / assertion failure) and NO previously-green test regresses.
