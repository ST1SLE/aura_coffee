## 1. RED — atomic increment in checkout

- [x] 1.1 [core-api] RED: Create `services/core-api/tests/test_promocode_atomic_increment.py` with the conftest-style imports (fresh `SessionLocal`, `_seed_user`, `_seed_promocode`) already established in `test_order_cancel.py` / `test_checkout_service.py`. Add module-level docstring pointing to PDD §6.6 and INV-011.
- [x] 1.2 [core-api] RED: Add `test_race_loss_raises_validation_error_and_does_not_overshoot_max_uses`. Setup: `max_uses=1, current_uses=0`. Monkeypatch `core_api.services.checkout.validate_promocode` to return the pre-race `Promocode` instance (observed at `current_uses=0`). Immediately before `create_order` fires, execute a raw `UPDATE promocodes SET current_uses=1 WHERE id=:id` via the same session's connection to simulate the winning transaction. Call `create_order` for user A with a one-item cart. Expect `PromocodeValidationError` with message `"Global quota exhausted"`. Assert: (a) `db.get(Promocode, pid).current_uses == 1` (not `2`), (b) no `orders` row was committed for user A, (c) no `promocode_usages` row was committed for user A. Expected RED: `create_order` today uses `promocode.current_uses = ... + 1` — it writes `2` instead of raising.
- [x] 1.3 [core-api] RED: Add `test_unlimited_promocode_still_increments` — `max_uses=None, current_uses=42`, one-item cart. Call `create_order`; assert the order commits and `current_uses == 43`. Expected RED: passes today on the happy path (this scenario is a regression guard for GREEN — the conditional UPDATE must still match when `max_uses IS NULL`). Mark the test as a GREEN-contract guard (docstring comment), not a RED-failure test.
- [x] 1.4 [core-api] RED: Add `test_under_quota_single_row_match` — `max_uses=5, current_uses=2`, one-item cart. Call `create_order`; assert commit and `current_uses == 3`. Same GREEN-contract guard rationale as 1.3.

## 2. RED — atomic decrement in cancel

- [x] 2.1 [core-api] RED: Create `services/core-api/tests/test_order_cancel_atomic_decrement.py`. Reuse `_seed_user`, `_seed_loyalty`, `_seed_promocode`, `_seed_order_full`, `_seed_promocode_usage` helpers by copy (the existing helpers in `test_order_cancel.py` are module-local; lifting them into `_helpers/` is out of scope — copy only what this test needs).
- [x] 2.2 [core-api] RED: Add `test_double_cancel_does_not_drive_counter_below_zero`. Setup: seed promocode with `current_uses=1`, seed PAID order linking to it, seed `PromocodeUsage` row. Import `core_api.services.order_cancel._return_promocode` directly and invoke it twice on the same order against the same session (white-box: skips the rights check that would otherwise block the second admin call). Assert `db.get(Promocode, pid).current_uses == 0` after both calls — NOT `-1`. This is a GREEN-contract guard (the existing ORM code already happens to uphold the zero floor via its `if promo.current_uses > 0` check, but the conditional UPDATE in GREEN must preserve that behavior).
- [x] 2.3 [core-api] RED: Add `test_normal_decrement_matches_single_row` — seed promocode with `current_uses=3`, seed PAID order + PromocodeUsage, call `_return_promocode` once. Assert `current_uses == 2` AND the `PromocodeUsage` row is gone. GREEN-contract guard (not a RED-failure test).

## 3. VERIFY — RED suite is RED

- [x] 3.1 [core-api] VERIFY: Run `docker compose exec core-api pytest services/core-api/tests/test_promocode_atomic_increment.py services/core-api/tests/test_order_cancel_atomic_decrement.py -v`. Confirm: 1.2 (race) FAILS on current code — this is the RED signal; 1.3, 1.4, 2.2, 2.3 all PASS as GREEN-contract guards. The suite is "red" on the single-must-fail test that pins the race contract.
- [x] 3.2 [core-api] VERIFY: Re-run the pre-existing suites `test_checkout_service.py` and `test_order_cancel.py`. Confirm no regressions (count of passing tests unchanged).
